{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window capability for the Move Party application shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
